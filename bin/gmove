#!/bin/bash

set -x

branch=$1
[ -z "$branch" ] && branch=master
echo "updating $branch..."

git fetch --all
git checkout $1
git fetch --all
git fetch --prune
git pull --rebase origin $1
git submodule update

echo
echo

git status
