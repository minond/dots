#!/bin/bash

# set -x

log() {
    local message=$1
    echo -e "\n- $message:"
}

branch=$1
[ -z "$branch" ] && branch=master

log "updating $branch"
git fetch --all
git fetch --prune
git checkout $branch
git pull --rebase origin $branch
git submodule update

log "status $branch for"
git status
